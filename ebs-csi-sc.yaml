apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs-csi-gp3
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: ebs.csi.aws.com # This is the critical change
volumeBindingMode: WaitForFirstConsumer
parameters:
  type: gp3
  encrypted: "true"
